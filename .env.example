NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
POSTMARK_SERVER_TOKEN=
POSTMARK_ACCOUNT_TOKEN=
POSTMARK_INBOUND_SECRET=
NEXT_PUBLIC_APP_URL=http://app.localhost:3000

# Stripe — billing / credit top-ups. Server-only, never expose the secret.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Stripe price ids backing the three credit bundles. Leave unset to hide
# a bundle from the billing page.
STRIPE_PRICE_STARTER=
STRIPE_PRICE_GROWTH=
STRIPE_PRICE_SCALE=
